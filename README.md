# xmip-core-authorize-location

Location authorization: whether this Location admits this identity at all — the Parties and identities each Receive or Send Location names, and the hours it keeps. A technology of [xmip-core-authorize](https://github.com/IlleNilsson/xmip-core-authorize).

## Toolchain

`rust-toolchain.toml` pins the toolchain for the whole estate. Do not change it
here.

## Verification

The included workflow is manual-only and calls the versioned shared workflow at
`IlleNilsson/.github@v1`.
